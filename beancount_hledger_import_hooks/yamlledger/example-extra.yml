---
rules:
- matchers:
  - and_is: description.is("from extra file")
  - or_is: description.is("some extra info")
  transforms:
  - set_to: category("Some extra category")
  - set_to: account("Some EXTRA account")
- matchers:
  - and_is: description.is("Drakes")
  transforms:
  - set_to: Account("Expenses:Food:Groceries:Drakes")
